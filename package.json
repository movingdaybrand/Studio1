{
  "scripts": {
    "build": "mkdir -p dist && for f in index.html experience.html guide.html client-guide.html; do [ -f \"$f\" ] && cp \"$f\" dist/; done; for d in clients shared; do [ -d \"$d\" ] && cp -R \"$d\" dist/; done; true"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.0"
  }
}
