[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/analyze-brand-files"
  to = "/.netlify/functions/analyze-brand-files"
  status = 200
